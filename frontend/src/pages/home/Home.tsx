import PropertyList from '../../components/property/list/PropertyList';

const Home = () => {
  return (
    <div className="container">
      <PropertyList />
    </div>
  );
};

export default Home;
